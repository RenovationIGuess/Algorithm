#include<stdio.h>
#include<stdlib.h>
#include<string.h>

int main()
{
    FILE *f;
    int *p;
    int i, n, value, sum;
    printf("Enter a list of number (with first num is the size): ");
    scanf("%d", n);
    p = (int *)malloc()
    return 0;
}