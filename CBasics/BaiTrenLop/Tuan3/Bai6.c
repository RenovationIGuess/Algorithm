#include<stdio.h>
#include<stdlib.h>


char *my_strcat(char *s1, char *s2)
{
    int len1, len2;
    char * result;
    
}

int main()
{
    char *s1, *s2;
    printf("Nhap xau 1: ");
    fflush(stdin);
    gets(&s1);
    printf("Nhap xau 2: ");
    fflush(stdin);
    gets(&s2);
    return 0;
}